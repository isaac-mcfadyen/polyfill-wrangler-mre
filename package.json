{
  "name": "polyfill-wrangler-mre",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.8.0",
    "typescript": "^4.6.4",
    "wrangler": "https://prerelease-registry.developers.workers.dev/prs/869/npm-package-wrangler-869"
  },
  "private": true,
  "scripts": {
    "publish:node": "wrangler publish --node-compat",
    "publish:vanilla": "wrangler publish"
  }
}
